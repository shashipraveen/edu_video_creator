from text_analysis import analyze_text  # Assuming analyze_text is the function in your text_analysis.py

sample_text = """Atoms are the building blocks of matter. Each element has a unique atomic structure. The periodic table organizes elements by atomic number."""

analysis = analyze_text(sample_text)
print("Analysis Output:", analysis)
